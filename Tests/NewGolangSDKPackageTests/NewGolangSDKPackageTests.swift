import XCTest
@testable import NewGolangSDKPackage

final class NewGolangSDKPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(NewGolangSDKPackage().text, "Hello, World!")
    }
}
